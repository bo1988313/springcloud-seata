spring:
  cloud:
    nacos:
      config:
        server-addr: localhost.nacos1:8848
      discovery:
        server-addr: localhost.nacos1:8848
    sentinel:
      transport:
        dashboard: localhost.nacos1:8080
#      datasource:
#        ds1:
#          nacos:
#            server-addr: localhost:80
#            dataId: ali-nacos-sentinel-seata-account
#            groupId: DEFAULT_GROUP
#            data-type: json
#            rule-type: flow
  application:
    name: ali-nacos-sentinel-seata-storage
management:
  endpoints:
    web:
      exposure:
        include: '*'